# 发行完整离线包时怎么做

当前是**开发布局**：前端在 `davinci-zimage`，引擎复用旁边的 `ComfyUI-zimage`。

## 目标布局

```
达芬七-ZImage-v1/
  启动.bat
  README.txt
  app/
  assets/
  engine/
    ComfyUI/
    python/
    （不要拷贝旧的 app.py / tab / 笨笨品牌文件）
```

## 步骤建议

1. 新建空目录 `达芬七-ZImage-v1`
2. 复制本仓库的 `app/`、`assets/`、`启动.bat`、`README.txt`
3. 从 `ComfyUI-zimage` 复制：
   - `python/`（完整）
   - `ComfyUI/`（可先全拷，再删）
4. 重命名为 `engine/python`、`engine/ComfyUI`
5. **裁剪**（显著减体积）：
   - 删除 `ComfyUI/output/*` 历史图
   - 删除未用模型：FlashVSR、SEEDVR2、flux1-fill、混元/视频相关
   - 删除 `assets` 里 openpose 图库（旧包）— 新包不依赖
   - 可选：只保留 styles.json 里出现的 LoRA
6. 用 8G 机器冷启动冒烟（见 plan 验收清单）
7. 全文搜索确认无「笨笨」「482421507」「xiaoniu」
8. 压缩上传网盘，X 发文带链接

## 环境变量

`启动.bat` 会优先找 `.\engine\ComfyUI\main.py`，因此发行布局无需再设 `ENGINE_ROOT`。

---

## 一键包怎么测（只有 16G Linux 时）

Windows 8G 是目标用户机；你若只有 16G Linux，可以这样测：

### A. 开发布局冒烟（现在就能做）

在 Linux 上把 `davinci-zimage` + 引擎拷过去（或 rsync），注意：

1. **引擎路径**：`启动.bat` 是 Windows 的；Linux 用手动启动：
   ```bash
   # 终端 1：ComfyUI
   cd /path/to/engine   # 或 ComfyUI-zimage
   ./python/bin/python -s ComfyUI/main.py --listen 127.0.0.1 --port 7777 --disable-auto-launch
   # 终端 2：UI
   cd /path/to/davinci-zimage
   export ENGINE_ROOT=/path/to/engine   # 若引擎不在 davinci-zimage/engine
   ./engine/python/bin/python app/main.py --server_name 127.0.0.1 --server_port 8888
   # 没有 engine/python 就用系统/自带 python，PYTHONPATH 指向 site-packages
   ```
2. 浏览器打开 `http://127.0.0.1:8888`（Comfy 后端 7777，不与笨笨 8188 冲突）
3. 清单：
   - [ ] 状态栏引擎在线
   - [ ] 1024 · 推荐 + 无风格 出 1 张
   - [ ] 选 1 个通用 LoRA 再出 1 张
   - [ ] 生成数量=3，中途点停止，应停在 ≤3 张
   - [ ] 收藏提示词 → 下拉能回填
   - [ ] 图库回填参数
   - [ ] GGUF 档位能否出图（可选）

16G 能过 **不代表** 8G 稳：分辨率默认用 **1024**，别用 1440 当 8G 结论。

### B. 真正「一键包」验收（推荐再找一台 8G Windows）

| 步骤 | 目的 |
|------|------|
| 解压到新目录，双击 `启动.bat` | 冷启动 |
| 不装任何依赖 | 离线 |
| 首次出图 | 模型加载 + 路径 |
| 换盘符/中文路径 | 路径坑 |
| 杀毒误报 | 发行前要知道 |

没有 8G Windows 时：用 **VM + 限制 GPU/显存** 很难模拟 CUDA 8G；最靠谱仍是借朋友 8G 本或云游戏/云桌面 Windows 实测一次。

### C. 发行前自检脚本思路

```text
1. engine/ComfyUI/main.py 存在
2. FP8 unet + clip + vae + 风格库里每个 LoRA 文件存在
3. python -c "from ui.app_ui import build_demo; build_demo()"
4. wait_comfy + 提交一张最小图
```

（可后续做成 `app/tools/smoke_pack.py`。）
