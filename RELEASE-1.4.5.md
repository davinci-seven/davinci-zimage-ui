# 达芬七 Z-Image · v1.4.5 风格增强补丁

状态：**开发中 · 代码已落地**（2026-08-02）  
定位：在 **v1.4.0 整包** 之上的 **内容 / 体验补丁**（可热更，不必重下 45G）  
版本号：`1.4.5`（正式版，见 `links.yaml`）。

**已含（相对 1.4.0）：**

| 项 | 状态 |
|----|------|
| 自定义尺寸 + 显存预警 | 已在 dev；含 512 / 1600 / 1920 / 2048 档 |
| LoRA / **提示词灵感** 双轨 | 已拆；可同开；默认分类「全部」 |
| 灵感库 **好看图 ~90** | 中文灵感名（非风格术语）；RSE-24h+Multiverse 真图；已剔同质材质卡 |
| 自建灵感 | **一键存当前提示词**；封面可选，不传自动文字卡 |
| 浏览页可收藏 | 「提示词灵感」「LoRA 风格」页均支持收藏/筛选 |
| 高级 · 自定义 LoRA | 登记 `models/loras` 为「我的」风格 |
| 图生图 | **不做**（进 1.5） |

---

## 0. 继承原则（旧规矩不丢）

与 `UPGRADE-BACKLOG-v1.5.md` / `HANDOFF-2026-08-02.md` / `docs/INSPIRATIONS.md` 一致：

| 原则 | 含义 |
|------|------|
| **傻瓜优先** | 功能可发现，不吓退小白；高级的进 Accordion |
| **8G 优先** | 默认路径在 8GB 可玩；预警优于静默 OOM |
| **仅 NVIDIA / CUDA** | 硬件口径不变 |
| **E 盘工程 · F 盘文章** | 媒体/代码原件在 E；文章在 F，勿整盘互拷 |
| **LoRA 用原生 LoraLoader** | 路径 Windows 反斜杠；比 rgthree 栈稳 |
| **Gradio 导出路径** | 生成图进 `userdata/exports` / allowed_paths |
| **启动 bat 禁中文括号** | ASCII bat + `start_ui.ps1` |
| **版本三处一致** | 发补丁时：`links.yaml` + README + 打包脚本 |
| **不侵权** | 外源风格：署名、链接、许可备注；样张本地重出 |

**本补丁额外纪律：**

1. **明确不要图生图**（img2img / ControlNet 等 → 留给 1.5.x）  
2. **提示词风格 vs LoRA 风格必须拆开**（见 §3）  
3. **外源风格必须本地小尺寸验证后才能上架**（见 §4）  
4. **注明出处 = 帮好友宣传**，不是白嫖提示词墙  

---

## 1. 一句话产品定义

> **v1.4.5 风格增强补丁**：更多达芬七预设 + 开源好友风格墙（本地验证、注明出处），**LoRA 与提示词风格分开**，支持收藏与自建。**不含图生图**。

对外口径示例：

- 已装 1.4.0 → 下 **1.4.5 补丁**（小体积）即可  
- 大功能（图生图、更重的编辑器、完整自定义尺寸发行等）→ **1.5.x**  

---

## 2. 范围

### 2.1 做（In scope）

| 块 | 内容 |
|----|------|
| **A. 风格二分** | UI/数据区分 **LoRA 风格** 与 **提示词风格**；点选行为不同（挂 LoRA vs 只改提示词） |
| **B. 达芬七精选** | 自有预设提示词；封面可参考 `F:\...\New folder` 精选 8 气质（霓虹惊悚、月光剪影、新艺术、裂纹漆金、早期网页、80 赛璐璐、PS1、黏土定格等）— **本地 Z-Image 重出封面** 再进包 |
| **C. 好友 / 开源墙** | 适配 [Punk-Skill](https://github.com/adrianpunk/Punk-Skill)、[Formulasearch 等 X 长文](https://x.com/Formulasearch/articles) 等；再收一小波 X 上优秀开源风格；**每条署名 + 链接** |
| **D. 本地验证** | GPT/云端向提示词 **全部** 用本包引擎、**最小尺寸**（建议 512² 或 720 长边）跑通；失败则砍或标「灵感/未适配」不进正式墙 |
| **E. 轻管理** | 用户：**收藏**、**自建提示词风格**、**删除自己的**；不改坏内置 JSON |
| **F. 可选代码** | 已在 dev 且稳定的小修可 cherry-pick（如自定义尺寸+显存预警、分辨率锁定交互）；**非必须**，以补丁体积与风险为准 |

### 2.2 不做（Out of scope · 明确）

- ❌ **图生图**（img2img）及任何「更多出图方法」大类  
- ❌ ControlNet / 视频 / 放大全家桶  
- ❌ 整包重打 45G 当唯一分发（补丁优先）  
- ❌ 未验证的 ChatGPT Image 2 / 云端原提示词直接上架  
- ❌ 声称「与 GPT Image / 原 Skill 像素级一致」  
- ❌ 语音 TTS、海外镜像、BT 主渠道（见 INSPIRATIONS）  
- ❌ 完整 CivitAI 下载器进用户主路径（可继续开发者工具）  

图生图与更重能力见：`UPGRADE-BACKLOG-v1.5.md` **第 3 项及以后**。

---

## 3. 数据与交互模型（草案）

### 3.1 分层

```
内置 LoRA 风格     assets/styles/styles.json          （现有 42，只读合并）
内置提示词风格     assets/styles/prompt_styles.json   （新建）
用户风格           userdata/styles_user.json          （可增删改）
用户封面           userdata/covers/                   （可选）
收藏               userdata/style_favorites.json      （或并入 settings）
```

### 3.2 类型字段（建议）

| kind | 生成时行为 |
|------|------------|
| `lora` | 现有：LoraLoader + trigger 注入 + 强度 |
| `prompt` | **不挂 LoRA**；将 `prompt_prefix` / `prompt_suffix`（及可选 negative）拼进用户提示词 |

公共字段示例：`id`, `name`, `cover`, `tags`, `categories[]`, `tip`, `featured`, `nsfw`  
LoRA 专有：`file`, `trigger`, `default_weight`, `civitai_url`, `commercial`  
Prompt 专有：`prompt_prefix`, `prompt_suffix`, `negative`（可选）, `source`（见下）

### 3.3 出处 / 好友字段（必填于外源）

```yaml
source:
  credit: "AdrianPunk · Punk-Skill"     # 展示名
  url: "https://github.com/adrianpunk/Punk-Skill"
  note: "封面向提示词灵感；已用 Z-Image 本地小尺寸验证重适配"
  license: "见上游仓库 / 页面"
```

UI：图卡或详情显示「灵感 · @xxx」可点链接。

### 3.4 UI（已落地）

- 风格区 **分页，不混墙**：**「LoRA 风格」|「提示词风格」|「收藏」**  
- 点 **提示词风格**：预览写明不加载 LoRA；强度对 prompt 自动灰掉  
- 点 **LoRA**：与 1.4 一致，可调强度  
- **提示词** Tab 内：另存当前提示词 / 删除用户项  
- 共用「当前选用」预览 + 清除 / 收藏  
- 对外文案：**不提图生图**

---

## 4. 本地验证规范（上架门槛）

每条拟上架的 **prompt 风格** 须满足：

| 检查 | 标准 |
|------|------|
| 引擎 | 本包 Comfy + 默认 FP8（或文档写明的档） |
| 尺寸 | **最小**：优先 512×512 或长边 720；通过后再可选 1024 样张 |
| 次数 | 至少 1 张可辨认「像该风格」；翻车严重 → 改 prompt 或弃用 |
| 封面 | 进包封面 = **本地出图**（可缩略），禁止只塞别人云端成片当「本包效果」 |
| 文案 | tip 写清「本地适配 · 非云端同款」 |
| 出处 | `source.credit` + `url` 非空（达芬七自有可写 `达芬七`） |

**验证记录**（建议目录，可后补）：

```
<开发树>\_dev_scratch\style-verify-1.4.5\
  <style_id>\
    meta.json      # 参数、seed、是否通过
    sample.png     # 最小尺寸样张
```

---

## 5. 内容候选池（待验证，非最终上架名单）

### 5.1 达芬七精选（气质参考）

路径：`F:\ClaudeCode\DaVinCi_AI\content\articles\30-drafts\writing\2026-07-28-达芬七ZImage整合包\New folder`

| 参考图 | 拟定方向（prompt 风格） |
|--------|-------------------------|
| 01 霓虹惊悚片 | 霓虹 / 惊悚片场 / 高对比 |
| 02 月光剪影 | 月光、剪影、安静叙事 |
| 03 新艺术彩窗 | 新艺术、彩窗、装饰线 |
| 04 裂纹漆金 | 裂纹漆、烫金、质感静物/人像 |
| 05 早期网页 UI | 早期网页、像素 UI、复古界面感 |
| 06 80 年代赛璐璐 | 80s 赛璐璐动画 |
| 07 PS1 低多边形 | PS1 / low poly |
| 08 黏土定格 | 黏土、定格动画 |

数量建议：**先 8 条验证通过再上**，可再扩。

### 5.2 好友 / 开源（需适配 + 署名）

| 来源 | 用途 | 注意 |
|------|------|------|
| [adrianpunk/Punk-Skill](https://github.com/adrianpunk/Punk-Skill) | 封面向风格 ID（杂志头版、黑白极简、方块世界…）抽关键词本地化 | 原为 Agent + 云端出图；**只搬可验证子集** |
| [Formulasearch Articles](https://x.com/Formulasearch/articles) 等 | 配方/风格叙述 → 抽 prompt 包 | 逐条验证；链回原文 |
| X 其他优秀开源风格 / skill | 同规则收录 | 控制总量 8～12 条好友向优先质量 |

**致谢区**（关于页 / README 补丁说明）：列出 credit 列表，强化「好友墙」而非竞品抄袭感。

### 5.3 数量上限（克制）

| 类别 | 建议首发数量 |
|------|----------------|
| 达芬七 prompt 精选 | 8～12 |
| 好友 / 开源 prompt | 8～12 |
| 现有 LoRA | 保持 42，不强制本补丁大扩 LoRA 文件 |

宁少勿滥；墙可读 > 墙很满。

---

## 6. 分发形态

| 项 | 建议 |
|----|------|
| 形态 | **补丁包**（zip）：`app/` 增量 + `assets/styles/*` + 说明 txt |
| 体积 | 目标尽量小（无新放大模型；封面 jpg 控制张数与分辨率） |
| 安装 | 解压覆盖整合包根目录 / 文档写清路径；保留 `userdata` |
| 版本号 | `links.yaml` → `1.4.5`；README / 补丁说明同步 |
| 与 1.5.0-dev | 开发树可继续堆 1.5；**打 1.4.5 补丁时从稳定提交 cherry-pick**，避免把半成品图生图打进去 |

---

## 7. 验收清单

### 功能

- [x] LoRA 与提示词风格在 UI 上可区分，行为正确（后者不加载 lora 文件）  
- [x] 内置 prompt 风格重启仍在；用户自建进 `userdata`，**不写坏**内置 json  
- [x] 收藏跨重启（`style_favorites.json`）  
- [x] 删除仅影响用户项  
- [x] 外源条目详情可见出处链接  
- [x] 关闭/未选风格时与 1.4.0 文生图行为一致（代码路径）  

### 内容

- [ ] 每条上架 prompt 风格有最小尺寸本地样张记录（**达芬七 8 张已有精选封面**；好友向仍用 default_card，建议你扫一眼出小样）  
- [x] 关于页或补丁说明有好友致谢列表  
- [x] 无「保证与 GPT Image 一致」类文案  

### 分发

- [ ] 补丁说明：从 1.4.0 升级步骤、不含水语音/图生图承诺  
- [ ] 版本号三处一致（现为 `1.4.5-dev`）  

### 代码落点摘要

- `assets/styles/prompt_styles.json` — 8 达芬七 + 9 好友/开源提示词风格  
- `app/core/styles.py` — kind 合并、收藏、用户 CRUD  
- `app/core/generate.py` — prompt 风格只注入提示词  
- `app/ui/app_ui.py` — 类型筛选 / 收藏 / 另存 / 删除  
- `OPEN-SOURCE.md` + 本目录 `git` 基线（tag `v1.4.5-dev`）

---

## 8. 建议实现顺序（本补丁内）

1. **数据层**：`prompt_styles.json` + `load_styles` 合并 kind；用户 json 路径  
2. **生成层**：`txt2img` 支持「仅 prompt 风格」注入，不走 LoraLoader  
3. **UI**：风格筛选 / 双类图卡；收藏；简单「另存为提示词风格」  
4. **内容**：达芬七 8 条验证 → 好友源逐条验证写入  
5. **关于 / README 致谢**  
6. **打补丁包 + 说明**  
7. （可选）cherry-pick 自定义尺寸等已稳代码  

风格库「完整编辑器 / CivitAI 半自动」仍属 **1.5 第 2 项** 深化；1.4.5 只做轻管理。

---

## 9. 与 v1.5 backlog 关系

| 文档 | 角色 |
|------|------|
| **本文件 `RELEASE-1.4.5.md`** | **下一对外补丁** · 风格生态 + 轻管理 · **无图生图** |
| `UPGRADE-BACKLOG-v1.5.md` | 中长期：自定义尺寸发行、完整风格编辑、**图生图** 等 |
| `HANDOFF-2026-08-02.md` | 总航向；两边维护 |

**顺序建议修订为：**

```
1.4.5 风格增强补丁（本文件）
  → 1.5.0 大功能（尺寸定稿发行 + 图生图 + 编辑器增强 …）
```

原 v1.5「实现顺序 1→3→2」中：  
- 第 1 项（自定义尺寸）代码已在 dev，可并 1.4.5 或 1.5 发行  
- **第 3 项图生图：明确不进 1.4.5**  
- 第 2 项风格编辑：1.4.5 做子集（收藏/自建 prompt），完整版留 1.5  

---

## 10. 对外文案备忘（发补丁时用）

- 先结果：多了哪些墙、好友致谢、本地可玩  
- 不讲编辑过程与内部扯皮  
- 一句边界：**没有图生图；需要的请等后续大版本**  
- 硬件仍是 **8G 显存不是内存**  

---

*Owner: 达芬七 · 与 HANDOFF-2026-08-02 / UPGRADE-BACKLOG-v1.5 联动 · 勿只留在聊天记录*
